N = int(input("Введіть число N: "))
sum_of_evens = 0
for number in range(1, N + 1):
    if number % 2 == 0:  # Перевірка, чи число парне
        sum_of_evens += number  # Додавання парного числа до суми
print(f"Сума всіх парних чисел від 1 до {N} дорівнює: {sum_of_evens}")
